<input id="header_display" type="checkbox" class="header_check_box">
<label for="header_display" class="header_open">▲</label>
<label for="header_display" class="header_back"></label>
<label for="sidebar_display" class="sidebar_open">◀︎</label>
<div class="header">
  <%= render "shared/header" %>
</div>

<div class="split_main">
  <input id="sidebar_display" type="checkbox" class="sidebar_check_box">
  <label for="sidebar_display" class="sidebar_back"></label>
  <div class="sidebar">
    <%= render "shared/side_bar" %>
  </div>
  <div class="main_screen">
    <h3 class="word_theme">
      お問い合わせ内容を入力してください
    </h3>

    <div class="word_board">
      <div class="word_board_text">
        【必ずお読みください】<br>
        ・質問やお困りごとは、こちらのページからお問い合わせください。<br>
        ・あなたのお名前、メールアドレス、問合せ内容を入力してください。<br>
        ・必要事項をすべて入力できましたら、確認ボタンを押してください。<br>
        ※このページでは、まだ内容が送信されることはありません。
      </div>
    </div>

    <%= form_with(model: @Inquiry,class: "inquiry_form", url: confirm_inquiry_path, local: true) do |form| %>
      <div class="inquiry_box">
        <%= render 'shared/error_messages', model: form.object %>

        <label>お名前</label>
        <%= form.text_field :name, value: @Inquiry.name, class:"inquiry_text_field", maxlength: 100, placeholder:"100文字以下で入力してください" %>
        <label>メールアドレス</label>
        <%= form.email_field :email, value: @Inquiry.email, class:"inquiry_text_field", maxlength: 100, placeholder:"100文字以下で入力してください" %>
        <label>お問い合わせ内容</label>
        <%= form.text_area :detail, value: @Inquiry.detail, class:"inquiry_area_field", maxlength: 1000, placeholder:"1000文字以下で入力してください" %>
        <%= form.submit '確認',class:"inquiry_blue_button", id: "blue_button" %>
      </div>
    <% end %>
  
  </div>
</div>